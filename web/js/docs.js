export async function loadDocs(owner,repo,root,branch='main'){const r=await fetch(`https://api.github.com/repos/${owner}/${repo}/git/trees/${encodeURIComponent(branch)}?recursive=1`,{headers:{Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error(`Docs tree HTTP ${r.status}`);const d=await r.json();const prefix=root.replace(/^\//,'').replace(/\/$/,'');return (d.tree||[]).filter(x=>x.type==='blob'&&x.path.startsWith(prefix+'/')&&x.path.toLowerCase().endsWith('.md')).map(x=>({path:x.path,raw:`https://raw.githubusercontent.com/${owner}/${repo}/${branch}/${x.path}`,name:x.path.split('/').pop(),relative:x.path.slice(prefix.length+1)}))}export async function loadDoc(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`Document HTTP ${r.status}`);return r.text()}
export async function loadConfig(){const r=await fetch('./web/data/config.json',{cache:'no-store'});if(!r.ok)throw new Error(`Configuration HTTP ${r.status}`);const t=await r.text();return JSON.parse(t.replace(/\/\/.*$/gm,'').replace(/,\s*([}\]])/g,'$1'))}export function joinUrl(base,path){if(/^https?:\/\//i.test(path))return path;return new URL(path.replace(/^\//,''),base.endsWith('/')?base:base+'/').href}
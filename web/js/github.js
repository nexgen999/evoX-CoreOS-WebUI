export async function getRepo(owner,name){const r=await fetch(`https://api.github.com/repos/${owner}/${name}`,{headers:{Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error(`GitHub repository HTTP ${r.status}`);return r.json()}export async function getProfile(owner){const r=await fetch(`https://api.github.com/users/${owner}`,{headers:{Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error(`GitHub user HTTP ${r.status}`);return r.json()}export async function getTree(owner,repo,path='',branch='main'){const r=await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${path}?ref=${encodeURIComponent(branch)}`,{headers:{Accept:'application/vnd.github+json'}});if(!r.ok)throw new Error(`GitHub contents HTTP ${r.status}`);return r.json()}
# 🌐 Déploiement

GitHub Pages sert directement les fichiers. La WebUI peut être séparée de CoreOS ou fusionnée dans son fork. Dans les deux cas, configurez `github.dataRepository` pour pointer vers le dépôt contenant JSON, RSS et docs.

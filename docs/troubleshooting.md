# 🛠️ Dépannage

Si l'écran est vide, vérifiez d'abord `index.html`, `web/css/style.css`, `web/js/app.js` et `web/data/config.json`. Ouvrez aussi la console du navigateur. Pour RSS/JSON externes, vérifiez que le serveur autorise les requêtes CORS. Pour GitHub, vérifiez le dépôt et la branche.

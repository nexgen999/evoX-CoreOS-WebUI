# Menu : About

Cette page est un exemple de documentation Markdown détectée automatiquement depuis `docs/`.

## Navigation

La documentation possède sa propre mini-sidebar. L'ordre peut être défini dans `config.json` avec `docs.order`.

## Personnalisation

Chaque menu peut être activé, renommé, déplacé et configuré indépendamment de la Home.

# 🏗️ Architecture

`index.html` est le point d'entrée. `web/css` contient le design. `web/js` contient le moteur. `web/data` contient la configuration. `web/assets` contient l'identité visuelle. Les données CoreOS restent externes et sont lues par HTTP.
